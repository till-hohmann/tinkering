{
  "_comment_paths": "Where Plaud staging files come from and where your vault lives. Absolute paths, or ~ for home.",
  "paths": {
    "recordings_dir": "~/PlaudRecordings",
    "vault_root": "~/ObsidianVault",
    "inbox_folder": "00_Inbox"
  },

  "_comment_fields": "Frontmatter field names this tool reads from each Plaud summary. Change if your Plaud templates use different names.",
  "fields": {
    "type": "type",
    "participant": "participant",
    "meeting_title": "meeting_title",
    "date": "date"
  },

  "_comment_routing": "How the 'type' field maps to a vault folder, filename prefix, and naming style. Order doesn't matter; the first matching type wins. Any 'type' not listed here is parked in the inbox_folder. Available naming styles: 'one_on_one' (uses Participant), 'titled' (uses Short Title), 'journal' (YYYY-MM-DD_Journal.md), 'inbox' (YYYY-MM-DD_Inbox_<Title>.md).",
  "routing": [
    {
      "type": "1on1",
      "folder": "10_Meetings/14_1on1",
      "prefix": "1on1",
      "naming": "one_on_one"
    },
    {
      "type": "general",
      "folder": "10_Meetings/11_General",
      "prefix": "GEN",
      "naming": "titled"
    },
    {
      "type": "journal",
      "folder": "20_Journal",
      "prefix": "",
      "naming": "journal"
    }
  ],

  "_comment_naming": "Knobs for the Short Title rule used by 'titled' routes.",
  "naming": {
    "short_title_max_words": 4,
    "strip_words": ["a", "an", "the", "and", "or", "with", "for", "in", "on", "at", "of", "to"]
  },

  "_comment_self_participant": "Names that count as 'yourself' in a 1-on-1. The first non-self participant becomes the filename.",
  "self_participant_names": ["You"],

  "_comment_indices": "Your Obsidian index files. The templater (plaud-template-sync) reads these to find canonical [[wikilinks]] for every person, company, project, etc. Each index file should use H2 (## Heading) sections to group entries by category. Paths are relative to vault_root.",
  "indices": [
    { "id": "people",    "path": "_Meta/Indices/People Index.md" },
    { "id": "companies", "path": "_Meta/Indices/Companies Index.md" }
  ],

  "_comment_templates_note": "A single markdown note in your vault where you keep your Plaud-side transcription templates. The templater only rewrites content between <!-- WIKILINK-RULES:<id> --> and <!-- /WIKILINK-RULES:<id> --> markers. You own the rest. Path is relative to vault_root.",
  "templates_note": "_Meta/PlaudTemplates.md",

  "_comment_templates": "Which indices and which categories feed each template's wikilink-rules block. 'index' must match an id in 'indices' above. 'categories' lists the H2 section names to include from that index, or use '*' for all categories in the index.",
  "templates": [
    {
      "id": "1on1",
      "include": [
        { "index": "people",    "categories": "*" },
        { "index": "companies", "categories": ["Customers"] }
      ]
    },
    {
      "id": "project_meeting",
      "include": [
        { "index": "people",    "categories": ["Team"] },
        { "index": "companies", "categories": "*" }
      ]
    }
  ],

  "_comment_corrections": "Optional. Find-and-replace pairs applied to every wikilink-rules block after rendering. Useful when an index file has a name that should be normalised before reaching the Plaud device (e.g. uppercase vs canonical casing). Leave empty if you don't need it.",
  "corrections": [],

  "_comment_relinker": "Knobs for plaud-relink, which finds plain-text mentions of note titles that should be [[wikilinks]] and fixes them. state_file is relative to vault_root. skip_prefixes / skip_exact exclude notes whose titles start with / equal a value (e.g. templates, dashboards). protected_paths is a list of vault-relative file paths that the relinker will never modify. min_title_length protects against false matches for very short titles. exclude_folders are vault-relative folder prefixes that will not be scanned for relinking.",
  "relinker": {
    "state_file": "_Meta/.relinker-state.json",
    "skip_prefixes": ["TPL ", "Dashboard "],
    "skip_exact": [],
    "protected_paths": [],
    "min_title_length": 4,
    "exclude_folders": ["_Meta", "_Assets"]
  },

  "_comment_graph_audit": "Knobs for plaud-graph-audit, the read-only health check that surfaces (a) wikilinks referenced anywhere in the vault but not listed in any index, (b) index entries with no inbound reference, (c) frequently-mentioned Title-Case phrases without their own note, (d) semantic notes that look stale (few backlinks + old). semantic_folders are the vault-relative folders considered 'concept notes' for the stale check. min_phrase_count is how many distinct notes a phrase must appear in to surface as a candidate. max_stale_backlinks + min_stale_age_days control the stale-note check.",
  "graph_audit": {
    "semantic_folders": ["People", "Companies"],
    "exclude_folders": ["_Meta", "_Assets"],
    "min_phrase_count": 3,
    "max_stale_backlinks": 2,
    "min_stale_age_days": 90
  }
}
