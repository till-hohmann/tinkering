---
date: 2026-04-18
---

# Untitled recording

This summary has no `type` field, so the ingester will park it in the inbox
folder for manual triage. A real Plaud output sometimes ships like this when
the template wasn't applied or the AI couldn't infer one.

Some content might still be salvageable — the inbox fallback exists so files
like this don't sit invisible in the staging folder forever.
